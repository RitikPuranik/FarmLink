import { z } from "zod";
const id=z.string().uuid(), unit=z.enum(["KG","QTL","TONNE"]), grade=z.enum(["A","B","C","D","REJECTED"]);
const location=z.object({latitude:z.number().min(-90).max(90).optional(),longitude:z.number().min(-180).max(180).optional()}).refine(x=>(x.latitude===undefined)===(x.longitude===undefined),"latitude and longitude must be together");
export const buyerProfileBody=z.object({organizationName:z.string().min(2).max(160),businessType:z.enum(["PROCESSOR","WHOLESALER","RETAILER","EXPORTER","INSTITUTIONAL_BUYER","TRADER","OTHER"]),description:z.string().max(2000).optional(),contactPerson:z.string().min(2).max(120),phone:z.string().min(7).max(30),email:z.string().email().optional(),address:z.string().max(500).optional(),state:z.string().min(2).max(100),district:z.string().min(2).max(100),website:z.string().url().optional()}).and(location);
export const demandBody=z.object({cropId:id,title:z.string().min(2).max(180),description:z.string().max(2000).optional(),requiredQuantity:z.number().positive(),minimumQuantity:z.number().positive().optional(),quantityUnit:unit,targetPrice:z.number().positive().optional(),minimumPrice:z.number().positive().optional(),maximumPrice:z.number().positive().optional(),qualityRequirements:z.object({grade:grade.optional(),moistureMax:z.number().nonnegative().max(100).optional(),foreignMatterMax:z.number().nonnegative().max(100).optional(),sizeRequirements:z.string().max(100).optional()}).strict().optional(),grade:grade.optional(),state:z.string().min(2).max(100),district:z.string().min(2).max(100),deliveryLocation:z.string().max(500).optional(),deliveryStartDate:z.coerce.date().optional(),deliveryEndDate:z.coerce.date().optional(),expiresAt:z.coerce.date().optional()}).and(location).superRefine((v,c)=>{if(v.minimumQuantity&&v.minimumQuantity>v.requiredQuantity)c.addIssue({code:z.ZodIssueCode.custom,message:"minimumQuantity cannot exceed requiredQuantity"});if(v.minimumPrice&&v.maximumPrice&&v.minimumPrice>v.maximumPrice)c.addIssue({code:z.ZodIssueCode.custom,message:"minimumPrice cannot exceed maximumPrice"});if(v.deliveryStartDate&&v.deliveryEndDate&&v.deliveryStartDate>v.deliveryEndDate)c.addIssue({code:z.ZodIssueCode.custom,message:"deliveryStartDate must precede deliveryEndDate"});});
export const offerBody=z.object({lotPublicId:id,buyerDemandPublicId:id.optional(),quantity:z.number().positive(),quantityUnit:unit,offeredPrice:z.number().positive(),deliveryTerms:z.string().max(1000).optional(),message:z.string().max(2000).optional(),expiresAt:z.coerce.date().optional()});
export const counterBody=offerBody.pick({quantity:true,quantityUnit:true,offeredPrice:true,deliveryTerms:true,message:true,expiresAt:true});
export const publicIdParams=z.object({publicId:id}); export const lotParams=z.object({lotPublicId:id});
